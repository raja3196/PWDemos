{
  "name": "pwdemos",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:master": "playwright test --grep @master",
    "test:sanity": "playwright test --grep @sanity",
    "test:regression": "playwright test --grep @regression",
    "test:master:headed": "playwright test --grep @master --headed",
    "test:sanity:debug": "playwright test --grep @master --debug"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "compilerOptions": {
    "types": [
      "node"
    ]
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.9.0"
  },
  "dependencies": {
    "csv-parse": "^6.2.1",
    "node": "^22.22.3"
  }
}
